# Inbox Usage

Each task is stored as a separate .md file with a descriptive kebab-case name.

Use `/check` to pull tasks from the main inbox or Reminders.app.
Use `/do` to view and select tasks to work on.
